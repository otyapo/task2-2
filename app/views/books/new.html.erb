<h1>New book</h1>
<%= form_for(@book) do |f| %>

	<h4>タイトル</h4>
	<%= f.text_field :title,class: "book_title" %>

	<h4>Body</h4>
	<%= f.text_area :body,class: "book_body" %>

	<%= f.submit 'creatbook' %>

	<% end %>